generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            Int       @id @default(autoincrement())
  name          String
  email         String
  passwordHash  String
}

model WitheboardNode {
  id            Int @id @default(autoincrement())
  title         String
  contentText   String
}
